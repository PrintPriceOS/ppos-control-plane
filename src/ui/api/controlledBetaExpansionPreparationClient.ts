export const evaluateExpansionPreparationReadiness = async (preparationId: string) => {
  return { readiness_status: 'READY' };
};

export const createExpansionPreparationGate = async (data: any) => {
  return {};
};
